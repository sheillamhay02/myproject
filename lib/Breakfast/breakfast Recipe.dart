import 'package:flutter/material.dart';

class BreakfastRecipeDetails extends StatelessWidget {
  const BreakfastRecipeDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
